spring:
  application:
    name: register-server
# 运行端口号8080
server:
  port: 8080
# 配置服务注册中心eureka
eureka:
  instance:
    hostname: localhost
  client:
    register-with-eureka: false
    fetch-registry: false