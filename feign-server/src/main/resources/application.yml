server:
  port: 8084
# 服务名称feign-server
spring:
  application:
    name: feign-server
# 连接服务注册中心eureka端口号8080
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8080/eureka/
feign:
  client:
    config:
      default:
        loggerLevel: full